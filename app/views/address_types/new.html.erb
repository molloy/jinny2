<h1>New address_type</h1>

<%= form_for(@address_type) do |f| %>
	<%= render "shared/error_messages", :target => @address_type %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', address_types_path %>