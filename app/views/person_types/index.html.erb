<h1>Listing person_types</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @person_types.each do |person_type| %>
  <tr>
    <td><%=h person_type.name %></td>
    <td><%= link_to 'Show', person_type %></td>
    <td><%= link_to 'Edit', edit_person_type_path(person_type) %></td>
    <td><%= link_to 'Destroy', person_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New person_type', new_person_type_path %>